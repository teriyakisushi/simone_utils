## Simone Utils
某工创赛（网联车算法设计）赛题的辅助工具库，不包含任何case的解决方案，仅提供一些常用的功能函数（比如障碍物、路径管理），方便快速对赛题case进行调试。

## 项目结构

- `headers/`：头文件目录，包含各个模块的接口定义。
- `src/`：源文件目录，包含各个模块的具体实现。

## 使用方法

将 `headers/` 和 `src/` 目录添加到你的代码项目中，并包含所需的头文件，然后调用相应的功能函数即可。

> 写的比较杂乱，部分功能非高性能和非最佳实践，仅供参考，本仓库不考虑更新和PR，谢谢理解。